<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.panotroller">
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Panotroller">
        <activity android:name=".ActivityPanoAcquisition"
            android:screenOrientation="portrait"/>
        <activity android:name=".ActivityPanoSetup"
            android:screenOrientation="portrait" />
        <activity android:name=".ActivityBluetoothConfig">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.panotroller.ActivityMain" />
        </activity>
        <activity android:name=".ActivityMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".BluetoothService">
            <intent-filter>
                <action android:name=".QuickService .BIND" />
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </service>
    </application>

</manifest>